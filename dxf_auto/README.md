# DXF-Auto

**Экспорт развёрток листовых деталей из КОМПАС-3D в DXF**

DXF-Auto — приложение для автоматизации экспорта развёрток листовых деталей из КОМПАС-3D в формат DXF для лазерной резки на станках с ЧПУ.

## Возможности

- 🔍 **Сканирование сборок** — автоматическое обнаружение листовых деталей в сборках
- 📋 **Дерево состава** — иерархическое отображение компонентов сборки
- 📊 **Таблица деталей** — список листовых деталей с фильтрацией по толщине
- ⚙️ **Гибкие настройки** — паттерны имён файлов, слои DXF, цвета линий
- 📤 **Пакетный экспорт** — экспорт всех выбранных деталей в один клик
- 📝 **Отчёты** — логирование операций и итоговый отчёт

## Требования

- Windows 10/11
- КОМПАС-3D v17+
- Python 3.8+

### Технические особенности

Приложение использует COM-автоматизацию через pywin32 с **динамической диспетчеризацией** 
(late-binding) для корректного доступа к интерфейсам КОМПАС-3D API. Это необходимо, так как 
интерфейс `IPart7` наследует свойства от `ISheetMetalContainer`, которые недоступны при 
раннем связывании (early-binding) через gen_py кэш.

## Установка

```bash
cd dxf_auto
pip install -r requirements.txt
```

## Запуск

```bash
python main.py
```

## Использование

1. Запустите КОМПАС-3D и откройте сборку (.a3d) или деталь (.m3d)
2. Запустите DXF-Auto
3. Нажмите "Сканировать" для поиска листовых деталей
4. Выберите детали для экспорта
5. Настройте параметры экспорта (опционально)
6. Нажмите "Экспорт DXF"

## Структура проекта

```
dxf_auto/
├── main.py           # Точка входа
├── config.py         # Конфигурация
├── core/             # Бизнес-логика
│   ├── kompas_api.py    # COM API обёртка
│   ├── assembly_scanner.py
│   └── dxf_exporter.py
├── models/           # Модели данных
│   ├── sheet_part.py
│   └── export_settings.py
├── ui/               # Интерфейс (Tkinter)
│   ├── main_window.py
│   ├── composition_tree.py
│   ├── sheet_table.py
│   ├── settings_dialog.py
│   └── export_dialog.py
└── resources/        # Ресурсы (иконки)
```

## Документация

- [KOMPAS API Reference](KOMPAS_API_REFERENCE.md) — справочник по API КОМПАС-3D

## Лицензия

MIT License

## Автор

DXF-Auto Team © 2024
